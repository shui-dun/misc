[user]
	name = shui-dun
	email = 2604697844@qq.com
[i18n]
	commitencoding = utf-8
	logoutputencoding = utf-8
[core]
	quotepath = false
	ignorecase = false
	editor = vim
	# for windows: 
	# editor = code-insiders --wait
[gui]
	encoding = utf-8
[credential]
	helper = store
[safe]
	# 避免仓库所有者不是当前用户时提交失败
	directory = *
# 代理设置（其实不需要设置，因为git bash会读取系统代理设置）
# [http]
# 	proxy = http://127.0.0.1:1081
# windows下的difftool设置
# [diff]
# 	tool = default-difftool
# [difftool "default-difftool"]
#     cmd = code-insiders --wait --diff $LOCAL $REMOTE
